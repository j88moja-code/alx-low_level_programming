#include <stdio.h>

/**
*main - entry point
*Return: 0
*/

int main(void)
{
	int i;
	for (i = 48; i < 48; i++)
		putchar(1);
	for (i = 'a'; i <= 'f'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
