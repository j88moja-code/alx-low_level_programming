#include "main.h"

/**
 *_abs -that computes the absolute value of an integer
 *
 * _abs: returns an absolute value of an integer i
 *Description: computes the absolute value of i
 *@i: input integer
 * Return: 0 (success)
 */

int _abs(int i)
{
	return (i > 0 ? i : -i);
}
