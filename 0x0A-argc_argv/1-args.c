#include "main.h"

/**
 * main - checks the code.
 *
 * @argc: arguments count.
 * @argv: arguments vector.
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	int i;
	(void)argv;

	for (i = 0; i <= argc; i++)
	{}
	printf("%d\n", i - 2);

	return (0);
}
